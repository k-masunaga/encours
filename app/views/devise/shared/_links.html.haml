- if controller_name != 'sessions'
  = link_to "ログイン(アカウントをお持ちの方)はこちら", new_session_path(resource_name), class: 'btn'
  %br/
- if devise_mapping.registerable? && controller_name != 'registrations'
  = link_to "新規登録はこちらから", new_registration_path(resource_name), class: 'btn'
  %br/
